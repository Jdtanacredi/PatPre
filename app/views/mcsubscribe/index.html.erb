<!-- <h3>Add a New Member</h3> -->
<!-- <p>Please enter your email address to subscribe to our newsletter.</p> -->
<%#= form_tag('/mcsubscribe/subscribe', method: "post", id: "mcsubscribe", remote: "true") do -%>
   <%#= email_field(:email, :address, {id: "email", placeholder: "email address"}) %>
   <%#= submit_tag("Subscribe") %>
<%# end %>
<!-- <div id="response">Response Will be displayed here</div> -->
<div class="clearfix patLabel">
	<%= image_tag('signup/Patmcgrath.png') %>
</div>


<div class="rx-only">
<p class="rx-tag">rx Only</p>
	<div class="" id="cmd">
	</div>
</div>

<script type="text/javascript">


var greeting = "ENTER YOUR EMAIL", 
    cursor = "<span>&#9646;</span>",
    cmd = $('#cmd');

function type(el,text,pos,no){
    ctext = text.substring(0,pos)+(pos%2?cursor:cursor);
    $(el).html(ctext);
    if(pos==text.length){
        $(el).html(text+cursor);
    }else{
        window.setTimeout('type("'+el+'","'+text+'",'+(pos+1)+','+1+');',70);
    }
}

$(document).ready(function(){
    cmd.append(cursor);
    type('#cmd',greeting,0);
})



	//$(document).ready(function() {
	//$(function() {
    //var BV = new $.BigVideo();
    //BV.init();
    //BV.show("<%= asset_path('Edit-3.mp4') %>");
//});
//});
</script>